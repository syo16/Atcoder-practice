#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    int n, a, b, c;
    cin >> n >> a >> b >> c;
    vector<int> l(n);
    int mp = 0;
    for (int i = 0; i < n; i++) {
        cin >> n[i];
    }

    for (int i = 0; i < n; i++) {
        int A = 0;
        int B = 0;
        int C = 0;
        for (int j = 0; j < 4; j++) { 
        }
    }
}
