#include <iostream>

using namespace std;

int main() {
    int N, K;
    cin >> N >> K;
    long long w[N];
    int p[N];
    long long z[N]
    for (int i = 0; i < N; i++) {
        cin >> w[i] >> p[i];
        z[i] = w[i] * p[i];
    }
    
    for (int j = 0; j < K; j++) {
        
    }
    
    return 0;
}